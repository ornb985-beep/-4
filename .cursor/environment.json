{
  "name": "Task Board (Vite + React + TS)",
  "install": "if [ -f package-lock.json ]; then npm ci; else npm install; fi",
  "terminals": [
    {
      "name": "dev",
      "command": "npm run dev"
    }
  ],
  "ports": [5173]
}
